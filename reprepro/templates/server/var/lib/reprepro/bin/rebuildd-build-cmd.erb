#!/bin/bash

d=$1
d=$(echo $d|sed 's/-proposed//g')
a=$2
p=$3
v=$4
r=$5

DEB_BUILD_OPTIONS="<%= buildoptions %>" /usr/local/bin/pbuilder-${d}-${a} build --logfile <%= scope.lookupvar('reprepro::params::homedir') %>/repos/${r}/logs/${p}-${v}-${a}.log ${p}_${v}.dsc
